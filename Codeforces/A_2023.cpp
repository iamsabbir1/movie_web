#include <bits/stdc++.h>
using namespace std;
void sovle()
{
    int n,k;
    cin >> n >> k;
    vector<int>b(n);
    for(int i=0;i<n;i++)
        cin >> b[i];
    
    
}

int main()
{
    int t;
    cin >> t;
    while(t--)
        solve();
}